machine:
  timezone:
    America/Denver
  node:
    version: 8.9.1

environment:
  DATABASE_URL: postgresql://ubuntu:@127.0.0.1:5432/palettepicker_test

database:
  override:
    - knex migrate:latest